{
  "name": "lambda-environment",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "invoke": "sls invoke -f commit-message-scheduler --logger",
    "invokeLocal": "sls invoke local -f commit-message-scheduler --logger"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "env-var": "^6.3.0"
  }
}
